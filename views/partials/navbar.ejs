<nav class="navbar navbar-expand-lg navbar-light fixed-top">
    <div class="container-fluid">
        <!-- Logo with soft floating effect -->
        <a class="navbar-brand logo-container" href="/">
            <div class="logo-soft-bg">
                <span class="logo-text">Vir<span class="logo-accent">cosa</span></span>
            </div>
        </a>
        
        <button class="navbar-toggler nav-toggler-soft" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="toggler-icon-line"></span>
            <span class="toggler-icon-line"></span>
            <span class="toggler-icon-line"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Navigation Menu with pill-style items -->
            <ul class="navbar-nav nav-main mx-auto">
                <li class="nav-item">
                    <a class="nav-link nav-item-pill" href="/">
                        <i class="fas fa-home nav-icon"></i>
                        <span class="nav-text">Home</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-item-pill" href="/products">
                        <i class="fas fa-spray-can nav-icon"></i>
                        <span class="nav-text">Perfumes</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-item-pill" href="/about-us">
                        <i class="fas fa-book-open nav-icon"></i>
                        <span class="nav-text">Our Story</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-item-pill" href="/contact-us">
                        <i class="fas fa-envelope nav-icon"></i>
                        <span class="nav-text">Contact</span>
                    </a>
                </li>
            </ul>
            
            <!-- User Actions -->
            <div class="nav-actions">
                <!-- Search Button -->
                <button class="nav-action-btn search-btn" id="searchToggleBtn">
                    <i class="fas fa-search"></i>
                    <span class="action-tooltip">Search</span>
                </button>
                
                <!-- Cart with floating bubble -->
                <a href="/cart" class="nav-action-btn cart-btn position-relative">
                    <i class="fas fa-shopping-bag"></i>
                    <span class="cart-count-bubble">0</span>
                    <span class="action-tooltip">Cart</span>
                </a>
                
                <!-- User/Login -->
                <a href="/login" class="nav-action-btn user-btn">
                    <i class="fas fa-user-circle"></i>
                    <span class="action-tooltip">Dashboard</span>
                </a>
            </div>
        </div>
    </div>
    
    <!-- Floating Navbar Effect -->
    <div class="nav-floating-effect"></div>
</nav>

<!-- Modern Glass Search Overlay -->
<div class="search-overlay" id="searchContainer">
    <div class="search-glass-container">
        <div class="search-header">
            <button class="search-close-btn" id="searchCloseBtn">
                <i class="fas fa-times"></i>
            </button>
            <div class="search-title">
                <i class="fas fa-search search-title-icon"></i>
                <span>Search Fragrances</span>
            </div>
        </div>
        
        <form class="search-glass-form" id="searchForm" action="/products/search" method="GET">
            <div class="search-input-container">
                <input 
                    type="text" 
                    class="search-glass-input" 
                    id="searchInput"
                    name="q"
                    placeholder="What fragrance are you looking for?"
                    autocomplete="off"
                >
                <div class="search-input-decoration">
                    <div class="search-wave"></div>
                </div>
                <button type="submit" class="search-glass-submit">
                    <i class="fas fa-arrow-right"></i>
                </button>
            </div>
        </form>

        <div class="search-trending">
            <span class="trending-label">Trending:</span>
            <div class="trending-tags">
                <button class="trending-tag" data-search="Citrus">Citrus Fresh</button>
                <button class="trending-tag" data-search="Floral">Floral Bouquet</button>
                <button class="trending-tag" data-search="Woody">Woody Notes</button>
                <button class="trending-tag" data-search="Oriental">Oriental Spice</button>
            </div>
        </div>

        <!-- Live Search Results -->
        <div class="search-results-glass" id="searchResults">
            <div class="results-loading" id="searchLoading">
                <div class="loading-pulse">
                    <div class="pulse-dot"></div>
                    <div class="pulse-dot"></div>
                    <div class="pulse-dot"></div>
                </div>
            </div>
            <div id="searchResultsContent"></div>
        </div>
    </div>
</div>

<style>
    :root {
        --nav-primary: #8B5A2B;
        --nav-secondary: #4A2C2A;
        --nav-accent: #D4AF8E;
        --nav-light: #F8F5F2;
        --nav-dark: #2A2118;
        --nav-glass: rgba(255, 255, 255, 0.9);
        --nav-shadow: rgba(139, 90, 43, 0.08);
        --nav-shadow-hover: rgba(139, 90, 43, 0.15);
    }

    /* Navbar Base */
    .navbar {
        background: var(--nav-glass);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        box-shadow: 0 8px 32px var(--nav-shadow);
        padding: 8px 0;
        border-radius: 24px;
        margin: 16px 20px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        position: fixed;
        width: calc(100% - 40px);
        top: 0;
        z-index: 1000;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .navbar:hover {
        box-shadow: 0 12px 48px var(--nav-shadow-hover);
        transform: translateY(-2px);
    }

    /* Logo with Soft Background */
    .logo-container {
        margin-left: 20px;
    }

    .logo-soft-bg {
        background: linear-gradient(135deg, rgba(139, 90, 43, 0.1), rgba(212, 175, 142, 0.1));
        padding: 6px 16px;
        border-radius: 20px;
        display: inline-block;
        backdrop-filter: blur(10px);
        border: 1px solid rgba(139, 90, 43, 0.1);
        transition: all 0.3s ease;
    }

    .logo-soft-bg:hover {
        background: linear-gradient(135deg, rgba(139, 90, 43, 0.15), rgba(212, 175, 142, 0.15));
        transform: scale(1.02);
    }

    .logo-text {
        font-family: 'Playfair Display', serif;
        font-size: 1.6rem;
        font-weight: 700;
        color: var(--nav-secondary);
        letter-spacing: -0.5px;
    }

    .logo-accent {
        color: var(--nav-primary);
        position: relative;
    }

    .logo-accent::after {
        content: '';
        position: absolute;
        bottom: 2px;
        left: 0;
        width: 100%;
        height: 6px;
        background: rgba(212, 175, 142, 0.3);
        border-radius: 3px;
        z-index: -1;
    }

    /* Navigation Menu Items */
    .nav-main {
        gap: 8px;
    }

    .nav-item-pill {
        padding: 8px 18px !important;
        border-radius: 14px;
        margin: 0 4px;
        color: var(--nav-dark);
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
    }

    .nav-item-pill::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(139, 90, 43, 0.08), rgba(212, 175, 142, 0.08));
        border-radius: 18px;
        transform: scaleX(0);
        transform-origin: right;
        transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        z-index: -1;
    }

    .nav-item-pill:hover::before {
        transform: scaleX(1);
        transform-origin: left;
    }

    .nav-item-pill.active::before {
        transform: scaleX(1);
        background: linear-gradient(135deg, rgba(139, 90, 43, 0.12), rgba(212, 175, 142, 0.12));
    }

    .nav-icon {
        font-size: 1.1rem;
        color: var(--nav-primary);
        transition: transform 0.3s ease;
    }

    .nav-item-pill:hover .nav-icon {
        transform: scale(1.2);
    }

    .nav-text {
        position: relative;
    }

    .nav-item-pill:hover .nav-text {
        animation: textGlow 0.3s ease;
    }

    @keyframes textGlow {
        0% { text-shadow: 0 0 0 rgba(139, 90, 43, 0); }
        100% { text-shadow: 0 0 8px rgba(139, 90, 43, 0.2); }
    }

    /* Action Buttons */
    .nav-actions {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-right: 20px;
    }

    .nav-action-btn {
        width: 48px;
        height: 48px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.6);
        border: 1px solid rgba(139, 90, 43, 0.1);
        color: var(--nav-primary);
        font-size: 1.2rem;
        position: relative;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        text-decoration: none;
    }

    .nav-action-btn:hover {
        background: white;
        transform: translateY(-4px);
        box-shadow: 0 8px 24px rgba(139, 90, 43, 0.15);
        border-color: var(--nav-accent);
    }

    .action-tooltip {
        position: absolute;
        bottom: -40px;
        left: 50%;
        transform: translateX(-50%);
        background: var(--nav-secondary);
        color: white;
        padding: 6px 12px;
        border-radius: 10px;
        font-size: 0.8rem;
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease;
        white-space: nowrap;
        pointer-events: none;
    }

    .action-tooltip::before {
        content: '';
        position: absolute;
        top: -6px;
        left: 50%;
        transform: translateX(-50%);
        border-left: 6px solid transparent;
        border-right: 6px solid transparent;
        border-bottom: 6px solid var(--nav-secondary);
    }

    .nav-action-btn:hover .action-tooltip {
        opacity: 1;
        visibility: visible;
        bottom: -35px;
    }

    /* Cart Bubble */
    .cart-count-bubble {
        position: absolute;
        top: -6px;
        right: -6px;
        background: linear-gradient(135deg, #FF6B6B, #FF8E8E);
        color: white;
        width: 22px;
        height: 22px;
        border-radius: 50%;
        font-size: 0.7rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: bubblePulse 2s infinite;
    }

    @keyframes bubblePulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }

    /* Custom Toggler */
    .nav-toggler-soft {
        width: 48px;
        height: 48px;
        border-radius: 14px;
        background: rgba(139, 90, 43, 0.08);
        border: 1px solid rgba(139, 90, 43, 0.1);
        padding: 0;
        position: relative;
        margin-right: 20px;
    }

    .toggler-icon-line {
        position: absolute;
        width: 24px;
        height: 2px;
        background: var(--nav-primary);
        border-radius: 1px;
        left: 50%;
        transform: translateX(-50%);
        transition: all 0.3s ease;
    }

    .toggler-icon-line:nth-child(1) { top: 16px; }
    .toggler-icon-line:nth-child(2) { top: 23px; }
    .toggler-icon-line:nth-child(3) { top: 30px; }

    .nav-toggler-soft[aria-expanded="true"] .toggler-icon-line:nth-child(1) {
        top: 23px;
        transform: translateX(-50%) rotate(45deg);
    }

    .nav-toggler-soft[aria-expanded="true"] .toggler-icon-line:nth-child(2) {
        opacity: 0;
    }

    .nav-toggler-soft[aria-expanded="true"] .toggler-icon-line:nth-child(3) {
        top: 23px;
        transform: translateX(-50%) rotate(-45deg);
    }

    /* Floating Effect */
    .nav-floating-effect {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle at 50% 0%, rgba(212, 175, 142, 0.1), transparent 70%);
        border-radius: 24px;
        pointer-events: none;
        z-index: -1;
    }

    /* Search Overlay */
    .search-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(42, 33, 24, 0.96);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        z-index: 9999;
        opacity: 0;
        visibility: hidden;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .search-overlay.active {
        opacity: 1;
        visibility: visible;
    }

    .search-glass-container {
        width: 90%;
        max-width: 800px;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(40px);
        -webkit-backdrop-filter: blur(40px);
        border-radius: 28px;
        padding: 40px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 20px 80px rgba(0, 0, 0, 0.3);
        transform: translateY(30px);
        transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .search-overlay.active .search-glass-container {
        transform: translateY(0);
    }

    .search-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 40px;
    }

    .search-title {
        display: flex;
        align-items: center;
        gap: 15px;
        color: white;
        font-size: 1.8rem;
        font-weight: 300;
    }

    .search-title-icon {
        color: var(--nav-accent);
        font-size: 2rem;
    }

    .search-close-btn {
        width: 56px;
        height: 56px;
        border-radius: 18px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        color: white;
        font-size: 1.5rem;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .search-close-btn:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: rotate(90deg);
    }

    /* Search Input */
    .search-input-container {
        position: relative;
        margin-bottom: 30px;
    }

    .search-glass-input {
        width: 100%;
        padding: 28px 70px 28px 30px;
        font-size: 1.4rem;
        background: rgba(255, 255, 255, 0.9);
        border: 2px solid rgba(139, 90, 43, 0.2);
        border-radius: 24px;
        color: var(--nav-dark);
        outline: none;
        transition: all 0.3s ease;
        font-family: 'Montserrat', sans-serif;
        font-weight: 400;
    }

    .search-glass-input:focus {
        background: white;
        border-color: var(--nav-accent);
        box-shadow: 0 0 40px rgba(212, 175, 142, 0.3);
    }

    .search-input-decoration {
        position: absolute;
        bottom: -10px;
        left: 20px;
        right: 20px;
        height: 4px;
        overflow: hidden;
        border-radius: 2px;
    }

    .search-wave {
        width: 200%;
        height: 100%;
        background: linear-gradient(90deg, 
            transparent, 
            var(--nav-accent), 
            transparent
        );
        animation: waveFlow 2s linear infinite;
        opacity: 0;
    }

    .search-glass-input:focus ~ .search-input-decoration .search-wave {
        opacity: 1;
    }

    @keyframes waveFlow {
        0% { transform: translateX(-100%); }
        100% { transform: translateX(100%); }
    }

    .search-glass-submit {
        position: absolute;
        right: 8px;
        top: 50%;
        transform: translateY(-50%);
        width: 62px;
        height: 62px;
        border-radius: 20px;
        background: linear-gradient(135deg, var(--nav-primary), var(--nav-accent));
        border: none;
        color: white;
        font-size: 1.4rem;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .search-glass-submit:hover {
        transform: translateY(-50%) scale(1.05);
        box-shadow: 0 8px 30px rgba(212, 175, 142, 0.4);
    }

    /* Trending Tags */
    .search-trending {
        margin-bottom: 40px;
    }

    .trending-label {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.9rem;
        margin-right: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .trending-tags {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        margin-top: 15px;
    }

    .trending-tag {
        padding: 10px 22px;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 20px;
        color: white;
        font-size: 0.9rem;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .trending-tag:hover {
        background: rgba(255, 255, 255, 0.2);
        transform: translateY(-2px);
    }

    /* Search Results */
    .search-results-glass {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        max-height: 400px;
        overflow-y: auto;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        display: none;
    }

    .search-results-glass.show {
        display: block;
    }

    .search-result-item {
        display: flex;
        align-items: center;
        padding: 18px 25px;
        border-bottom: 1px solid rgba(139, 90, 43, 0.1);
        text-decoration: none;
        color: var(--nav-dark);
        transition: all 0.3s ease;
    }

    .search-result-item:hover {
        background: rgba(139, 90, 43, 0.05);
        padding-left: 30px;
    }

    .search-result-img {
        width: 64px;
        height: 64px;
        object-fit: cover;
        border-radius: 14px;
        margin-right: 20px;
        border: 2px solid rgba(139, 90, 43, 0.1);
    }

    .search-result-info {
        flex: 1;
    }

    .search-result-name {
        font-weight: 600;
        color: var(--nav-secondary);
        margin-bottom: 5px;
        font-size: 1.1rem;
    }

    .search-result-category {
        font-size: 0.85rem;
        color: var(--nav-primary);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .search-result-price {
        font-weight: 700;
        color: var(--nav-primary);
        font-size: 1.2rem;
    }

    /* Loading Animation */
    .loading-pulse {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        padding: 30px;
    }

    .pulse-dot {
        width: 12px;
        height: 12px;
        background: var(--nav-accent);
        border-radius: 50%;
        animation: pulse 1.4s ease-in-out infinite;
    }

    .pulse-dot:nth-child(2) { animation-delay: 0.2s; }
    .pulse-dot:nth-child(3) { animation-delay: 0.4s; }

    @keyframes pulse {
        0%, 60%, 100% { transform: scale(1); opacity: 0.4; }
        30% { transform: scale(1.3); opacity: 1; }
    }

    /* Responsive Design */
    @media (max-width: 992px) {
        .navbar {
            margin: 12px;
            width: calc(100% - 24px);
            border-radius: 20px;
        }
        
        .nav-main {
            margin: 20px 0;
        }
        
        .nav-item-pill {
            padding: 10px 20px !important;
            margin: 4px 0;
        }
        
        .logo-soft-bg {
            padding: 8px 16px;
        }
        
        .logo-text {
            font-size: 1.6rem;
        }
    }

    @media (max-width: 768px) {
        .search-glass-container {
            padding: 25px;
            border-radius: 20px;
        }
        
        .search-title {
            font-size: 1.4rem;
        }
        
        .search-glass-input {
            padding: 20px 60px 20px 25px;
            font-size: 1.1rem;
        }
        
        .search-glass-submit {
            width: 50px;
            height: 50px;
            border-radius: 16px;
        }
        
        .trending-tags {
            justify-content: center;
        }
    }

    @media (max-width: 576px) {
        .nav-actions {
            gap: 8px;
        }
        
        .nav-action-btn {
            width: 42px;
            height: 42px;
            font-size: 1rem;
        }
        
        .cart-count-bubble {
            width: 18px;
            height: 18px;
            font-size: 0.6rem;
        }
    }
</style>

<script>
    // Search functionality
    const searchToggleBtn = document.getElementById('searchToggleBtn');
    const searchContainer = document.getElementById('searchContainer');
    const searchCloseBtn = document.getElementById('searchCloseBtn');
    const searchInput = document.getElementById('searchInput');
    const searchResults = document.getElementById('searchResults');
    const searchResultsContent = document.getElementById('searchResultsContent');
    const searchLoading = document.getElementById('searchLoading');
    const searchForm = document.getElementById('searchForm');

    let searchTimeout;

    // Open search
    searchToggleBtn.addEventListener('click', (e) => {
        e.preventDefault();
        searchContainer.classList.add('active');
        document.body.style.overflow = 'hidden';
        setTimeout(() => searchInput.focus(), 400);
    });

    // Close search
    function closeSearch() {
        searchContainer.classList.remove('active');
        document.body.style.overflow = '';
        searchInput.value = '';
        searchResults.classList.remove('show');
        searchResultsContent.innerHTML = '';
    }

    searchCloseBtn.addEventListener('click', closeSearch);

    // Close on background click
    searchContainer.addEventListener('click', (e) => {
        if (e.target === searchContainer) {
            closeSearch();
        }
    });

    // Close on ESC key
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && searchContainer.classList.contains('active')) {
            closeSearch();
        }
    });

    // Live search functionality
    searchInput.addEventListener('input', (e) => {
        const query = e.target.value.trim();

        clearTimeout(searchTimeout);

        if (query.length < 2) {
            searchResults.classList.remove('show');
            return;
        }

        // Show loading
        searchLoading.classList.add('show');
        searchResults.classList.add('show');
        searchResultsContent.innerHTML = '';

        // Debounce search
        searchTimeout = setTimeout(async () => {
            try {
                const response = await fetch(`/api/products/search?q=${encodeURIComponent(query)}`);
                const data = await response.json();

                searchLoading.classList.remove('show');

                if (data.products && data.products.length > 0) {
                    displaySearchResults(data.products);
                } else {
                    displayNoResults(query);
                }
            } catch (error) {
                console.error('Search error:', error);
                searchLoading.classList.remove('show');
                displayNoResults(query);
            }
        }, 300);
    });

    // Display search results
    function displaySearchResults(products) {
        searchResultsContent.innerHTML = products.map(product => `
            <a href="/products/product-details/${product._id}" class="search-result-item">
                <img src="${product.images[0]}" alt="${product.name}" class="search-result-img">
                <div class="search-result-info">
                    <div class="search-result-name">${product.name}</div>
                    <div class="search-result-category">${product.categories.join(', ')}</div>
                </div>
                <div class="search-result-price">PKR ${product.variants[0].price}</div>
            </a>
        `).join('');
    }

    // Display no results
    function displayNoResults(query) {
        searchResultsContent.innerHTML = `
            <div class="no-results">
                <i class="fas fa-search"></i>
                <p>No fragrances found for "<strong>${query}</strong>"</p>
                <p style="font-size: 0.9rem; color: #999; margin-top: 10px;">
                    Try different keywords or browse all products
                </p>
            </div>
        `;
    }

    // Prevent form submission if clicking on result
    searchResultsContent.addEventListener('click', (e) => {
        if (e.target.closest('.search-result-item')) {
            // Let the link navigate naturally
            closeSearch();
        }
    });

    updateCartCount(3);
</script>

