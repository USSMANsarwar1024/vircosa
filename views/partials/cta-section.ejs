<!-- CTA Section -->
<section class="cta-section">
    <div class="cta-overlay"></div>
    <div class="container text-center">
        <h2 class="cta-title">Own the Scent. Own the Moment.</h2>
        <p class="cta-text">
            This isn't just perfume — it's confidence, charm, and class bottled for you. 
            Because some experiences aren't meant to be compared by price — only by how they make you feel.
        </p>
        <a href="/products" class="btn btn-cta">Experience Vircosa</a>
    </div>
</section>

<style>
/* ===== CTA Section ===== */
.cta-section {
    position: relative;
    padding: 60px 0;
    background: url("../images/login-hero-img.avif") no-repeat center center;
    background-size: cover;
    background-attachment: fixed;
    color: #ffffff;
    text-align: center;
    overflow: hidden;
}

.cta-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(42, 33, 24, 0.85), rgba(42, 33, 24, 0.75));
    z-index: 1;
}

.cta-section .container {
    position: relative;
    z-index: 2;
}

.cta-title {
    font-size: 3rem;
    font-weight: 600;
    margin-bottom: 24px;
    letter-spacing: 0.5px;
    line-height: 1.2;
}

.cta-text {
    font-size: 1.125rem;
    max-width: 680px;
    margin: 0 auto 48px;
    opacity: 0.95;
    line-height: 1.7;
    font-weight: 300;
}

.btn-cta {
    display: inline-block;
    background-color: var(--accent-color);
    color: var(--secondary-color);
    padding: 14px 42px;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1rem;
    letter-spacing: 0.5px;
    text-decoration: none;
    border: 2px solid transparent;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

.btn-cta:hover {
    background-color: #4a2c2a;
    transform: translateY(-2px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
    border-color: rgba(255, 255, 255, 0.1);
}

/* Responsive Design */
@media (max-width: 768px) {
    .cta-section {
        padding: 100px 0;
        background-attachment: scroll;
    }
    
    .cta-title {
        font-size: 2.2rem;
    }
    
    .cta-text {
        font-size: 1rem;
        padding: 0 20px;
    }
    
    .btn-cta {
        padding: 12px 36px;
    }
}

@media (max-width: 480px) {
    .cta-title {
        font-size: 1.8rem;
    }
}
</style>